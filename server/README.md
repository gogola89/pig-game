# Http Server to serve built or raw html project files

## Available Scripts

In the project directory, you can run:

### `npm start`

## Project files

### Place your static files in `public` folder as mounted in `app.js`
